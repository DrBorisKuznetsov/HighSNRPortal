{
  "name": "highsnr-lab-workspace",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "check": "npm run check:portal && npm run check:videos && npm run check:adc && npm run check:capacitor",
    "check:portal": "npm --prefix portal run check",
    "check:videos": "npm --prefix videos_project run lint && npm --prefix videos_project run build",
    "check:adc": "cd adc_tool && python -m unittest discover -s tests",
    "check:capacitor": "npm --prefix capacitor_filter_tool run build"
  }
}
